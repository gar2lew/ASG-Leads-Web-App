{
  "current_goal": "Post-Sprint-2 Consolidation PR Preparation",
  "branch": "goal/post-sprint-2-consolidation",
  "source_branch": "fix/functions-emulator-startup-investigation",
  "source_commit": "b6fdb5a",
  "base_branch": "origin/codex-foundation",
  "base_commit": "04d21e8",
  "source_savepoint": "savepoint-before-post-sprint-2-consolidation",
  "application_savepoint": "savepoint-before-post-sprint-2-commit-application",
  "applied_commit_count": 27,
  "conflicts": [],
  "tree_comparison": {
    "compared_to": "origin/fix/functions-emulator-startup-investigation",
    "result": "match before final consolidation documentation updates"
  },
  "preserved": [
    "UAT.md",
    "handoff.json",
    "docs/UAT",
    "dependency remediation package files",
    "Quick Pull import date and limit fix",
    "Functions emulator investigation",
    "AI handoff files",
    "repository cleanup audit files"
  ],
  "proposed_pr": {
    "title": "v1.0 hardening, UAT preparation and lead import fixes",
    "base": "codex-foundation",
    "head": "goal/post-sprint-2-consolidation",
    "status": "prepared only, not pushed and not created"
  },
  "validation": {
    "status": "passed through functions settings-admin; final git checks pending",
    "finding": "Callable emulator initially failed because ignored functions/lib output was stale after consolidation. Rebuilding functions refreshed lib output and callable dry-run passed.",
    "completed": [
      "npm.cmd run typecheck",
      "npm.cmd run lint",
      "npm.cmd test",
      "npm.cmd run build",
      "npm.cmd run test:emulator:preflight",
      "npm.cmd run test:emulator:rules",
      "npm.cmd run test:emulator:firestore-smoke",
      "cd functions && npm.cmd run build",
      "npm.cmd run test:emulator:callables-dry-run",
      "cd functions && npm.cmd run test:settings-admin"
    ],
    "commands": [
      "npm run typecheck",
      "npm run lint",
      "npm test",
      "npm run build",
      "npm run test:emulator:preflight",
      "npm run test:emulator:rules",
      "npm run test:emulator:firestore-smoke",
      "npm run test:emulator:callables-dry-run",
      "cd functions && npm run build",
      "cd functions && npm run test:settings-admin",
      "git diff --check",
      "git status",
      "git fsck --full"
    ]
  },
  "safety_rules": [
    "Do not deploy.",
    "Do not push without explicit approval.",
    "Do not create the PR without explicit approval.",
    "Do not merge, retarget, rebase, reset, or force-push shared branches.",
    "Do not delete branches or stashes without explicit approval.",
    "Do not move or delete existing release tags.",
    "Do not alter production Firebase, Firestore rules, Firebase config, migrations, Salestrail live sync, or phone backfill writes."
  ],
  "rollback": {
    "to_clean_base": "git reset --hard savepoint-before-post-sprint-2-commit-application",
    "to_source_branch": "git switch fix/functions-emulator-startup-investigation",
    "source_branch_reset_requires_approval": "git reset --hard savepoint-before-post-sprint-2-consolidation"
  }
}
